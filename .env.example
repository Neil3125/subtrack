# Database
# For local development, use SQLite (default):
DATABASE_URL=sqlite:///./subtrack.db

# For Railway deployment with PostgreSQL (RECOMMENDED):
# 1. Add a PostgreSQL service in Railway dashboard
# 2. Railway automatically sets DATABASE_URL - no manual config needed!
# 3. Your data persists across all code deployments automatically
# Example: DATABASE_URL=postgresql://user:password@host:port/database

# AI Provider (optional - app works without it)
# Choose provider: "openrouter" (recommended), "gemini", or "openai"
SUBTRACK_AI_PROVIDER=openrouter
SUBTRACK_AI_API_KEY=
# For OpenRouter (recommended - free tier with 50 requests/day):
#   Model: google/gemini-2.0-flash-001:free
#   Get your free key: https://openrouter.ai/
# For Gemini: gemini-pro, gemini-pro-vision
# For OpenAI: gpt-4, gpt-3.5-turbo
SUBTRACK_AI_MODEL=google/gemini-2.0-flash-exp:free
# Base URL for API (OpenRouter or OpenAI-compatible)
SUBTRACK_AI_BASE_URL=https://openrouter.ai/api/v1

# AI Cache Settings (optional)
# Cache TTL in seconds (default: 86400 = 24 hours)
AI_CACHE_TTL=86400
# Request timeout in seconds (default: 30)
AI_REQUEST_TIMEOUT=30

# App Settings
DEBUG=true
SECRET_KEY=your-secret-key-change-in-production
