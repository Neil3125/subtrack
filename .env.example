# Database
# For local development, use SQLite (default):
DATABASE_URL=sqlite:///./subtrack.db

# For Railway deployment with PostgreSQL (RECOMMENDED):
# 1. Add a PostgreSQL service in Railway dashboard
# 2. Railway automatically sets DATABASE_URL - no manual config needed!
# 3. Your data persists across all code deployments automatically
# Example: DATABASE_URL=postgresql://user:password@host:port/database

# AI Provider - Google Gemini API (free tier available)
# Get your API key: https://aistudio.google.com/app/apikey
SUBTRACK_AI_PROVIDER=gemini
SUBTRACK_AI_API_KEY=your-gemini-api-key-here
# Available models (examples): gemini-2.0-flash, gemini-2.5-flash
# Pick a Gemini model that is enabled for your API key.
SUBTRACK_AI_MODEL=gemini-2.0-flash
SUBTRACK_AI_BASE_URL=https://generativelanguage.googleapis.com/v1beta

# AI Cache Settings (optional)
# Cache TTL in seconds (default: 86400 = 24 hours)
AI_CACHE_TTL=86400
# Request timeout in seconds (default: 30)
AI_REQUEST_TIMEOUT=30

# App Settings
DEBUG=true
SECRET_KEY=your-secret-key-change-in-production

# Email Settings (for renewal notifications via Resend)
# Sign up at https://resend.com/ (100 emails/day free)
# Get your API key from: Resend Dashboard > API Keys
RESEND_API_KEY=re_xxxxxxxxxxxx
# From email - MUST use a verified domain to send to external recipients
# Example: notifications@yourdomain.com (verify domain at resend.com/domains)
RESEND_FROM_EMAIL=notifications@neil.service.com
RESEND_FROM_NAME=SubTrack Notifications
