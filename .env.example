# Database
# For local development, use SQLite (default):
DATABASE_URL=sqlite:///./subtrack.db

# For Railway deployment with PostgreSQL (RECOMMENDED):
# 1. Add a PostgreSQL service in Railway dashboard
# 2. Railway automatically sets DATABASE_URL - no manual config needed!
# 3. Your data persists across all code deployments automatically
# Example: DATABASE_URL=postgresql://user:password@host:port/database

# AI Provider (optional - app works without it)
# Choose provider: "github_models" (FREE - recommended), "anthropic", "groq", "huggingface", "openrouter", "gemini", or "openai"
SUBTRACK_AI_PROVIDER=github_models
SUBTRACK_AI_API_KEY=
# For GitHub Models (FREE - unlimited with GitHub token):
#   Get your token: https://github.com/settings/tokens (create Personal Access Token)
#   Models: gpt-4o, gpt-4-turbo, llama-2-70b, mistral-large
#   Token format: github_pat_...
# For Anthropic Claude (pay-as-you-go, very cheap): 
#   Key format: sk-ant-api03-...
# For Hugging Face (30,000 requests/month):
#   Model: meta-llama/Llama-2-7b-chat-hf
#   Get your free key: https://huggingface.co/settings/tokens
# For OpenRouter: google/gemini-2.0-flash-001, google/gemma-2-9b-it:free
# For Gemini: gemini-pro, gemini-pro-vision
# For OpenAI: gpt-4, gpt-3.5-turbo
SUBTRACK_AI_MODEL=gpt-4o
# Base URL for API
SUBTRACK_AI_BASE_URL=https://models.github.ai/inference

# AI Cache Settings (optional)
# Cache TTL in seconds (default: 86400 = 24 hours)
AI_CACHE_TTL=86400
# Request timeout in seconds (default: 30)
AI_REQUEST_TIMEOUT=30

# App Settings
DEBUG=true
SECRET_KEY=your-secret-key-change-in-production
